# ACCEPTANCE — PureProbe 验收标准

## v1.0 验收清单（全部满足才算通过）

### 功能
- [ ] 添加订阅：粘贴机场 URL，解析出节点并显示数量（订阅 URL 加 `flag=meta` 参数自动拼接尝试）
- [ ] 一键体检：进度实时显示（已测/总数/当前节点名），可中途停止
- [ ] 三层结果全部落榜：
  - L1 死节点 → 状态"不通"
  - L2 Google 204 不通 → 状态"污染"
  - L3 出口 IP 风险分（ip-api 代理/主机/风险字段）→ 状态"高风险"
- [ ] 排行榜：干净节点置顶，按延迟排序，显示节点名/延迟/出口IP/风险等级
- [ ] 缓存：同一订阅 24h 内二次体检只测新增/上次失败节点（增量）
- [ ] 导出：一键复制全部干净节点名 + 延迟列表（供手动在代理客户端里对照保留）

### 安全与流量
- [ ] 并发默认 8，设置页可调 1-16
- [ ] 单节点单轮请求 ≤3 个（204 级别）
- [ ] 订阅 URL 不出现在任何日志输出中（token 打码验证）
- [ ] App 杀进程后 mihomo 子进程无残留（ps 验证）

### 工程
- [ ] preflight 全绿（治理文档齐、无超 1500 行文件、无未转义 innerHTML、调试面板存在）
- [ ] Skills 检查门禁：webapp-testing + dom-static-check 全过
- [ ] GitHub Actions 构建：apksigner verify + unzip -tq + SHA-256 报告
- [ ] 手机安装实测：真机跑通一次全量体检（主人操作）

## v2 候选（不在 v1 承诺内）
- 下载测速层（幸存者限流测速）
- Clash 配置片段导出（url-test 分组）
- 定时自动体检（WorkManager）
- IP 纯净度升级（scamalytics 评分）

## 交付物
1. Release APK（GitHub Release 资产）
2. 交付报告：改动文件绝对路径、备份路径、技能检查结果、SHA-256、未解决问题
